---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "graphiant_assurance_global Resource - terraform-provider-graphiant"
subcategory: ""
description: |-
  A global SLA assurance configuration (data-assurance domain): which apps/LANs/sites to monitor and which flex algo to score against.
---

# graphiant_assurance_global (Resource)

A global SLA assurance configuration (data-assurance domain): which apps/LANs/sites to monitor and which flex algo to score against.

## Example Usage

```terraform
resource "graphiant_assurance_global" "critical_apps" {
  name          = "critical-apps"
  use_all_sites = true

  lan_names = ["corp-lan"]

  apps = [
    { custom_app_id = graphiant_custom_app.example.id },
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `apps` (Attributes List) (see [below for nested schema](#nestedatt--apps))
- `flex_algo` (String)
- `lan_names` (List of String)
- `name` (String)
- `site_list_id` (Number) Mutually exclusive with use_all_sites (not enforced by a validator).
- `use_all_sites` (Boolean)

### Read-Only

- `id` (String) The ID of this resource.

<a id="nestedatt--apps"></a>
### Nested Schema for `apps`

Optional:

- `builtin_app_id` (Number)
- `custom_app_id` (Number)
- `is_domain` (Boolean)
- `name` (String)
- `use_all_servers` (Boolean)

Read-Only:

- `bucket_id` (Number)

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import graphiant_assurance_global.critical_apps <assurance_id>
```
