---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "graphiant_alert_integration Resource - terraform-provider-graphiant"
subcategory: ""
description: |-
  An alert delivery integration (Zendesk/Slack webhook/PagerDuty/Opsgenie/Opsramp). Only one field under details should be set, matching integration_type — the API validates this server-side, there is no closed enum for integration_type in the SDK. Import uses ":" since Read needs the owning enterprise id, which the API doesn't echo back on the integration object itself.
---

# graphiant_alert_integration (Resource)

An alert delivery integration (Zendesk/Slack webhook/PagerDuty/Opsgenie/Opsramp). Only one field under details should be set, matching integration_type — the API validates this server-side, there is no closed enum for integration_type in the SDK. Import uses "<enterprise>:<id>" since Read needs the owning enterprise id, which the API doesn't echo back on the integration object itself.

## Example Usage

```terraform
resource "graphiant_alert_integration" "oncall_webhook" {
  enterprise       = 12345
  integration_type = "webhook"
  nick_name        = "oncall-webhook"
  is_active        = true

  details = {
    webhook_url = "https://hooks.example.com/oncall"
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `enterprise` (Number)
- `integration_type` (String)
- `nick_name` (String)

### Optional

- `details` (Attributes) (see [below for nested schema](#nestedatt--details))
- `is_active` (Boolean)

### Read-Only

- `id` (String) The ID of this resource.

<a id="nestedatt--details"></a>
### Nested Schema for `details`

Optional:

- `opsgenie_key` (String, Sensitive)
- `opsramp_details` (String, Sensitive)
- `pagerduty_routing_key` (String, Sensitive)
- `webhook_url` (String)
- `zendesk_details` (Attributes) (see [below for nested schema](#nestedatt--details--zendesk_details))

<a id="nestedatt--details--zendesk_details"></a>
### Nested Schema for `details.zendesk_details`

Optional:

- `zendesk_assignee_id` (String)
- `zendesk_base_url` (String)
- `zendesk_client_id` (String)
- `zendesk_client_secret` (String, Sensitive)

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import graphiant_alert_integration.oncall_webhook <integration_id>
```
